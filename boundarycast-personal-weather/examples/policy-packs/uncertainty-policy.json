{
  "policy_pack_id": "uncertainty-policy-v1",
  "version": "1.0.0",
  "domain": "weather",
  "rules": [
    {
      "rule_id": "UNCERTAINTY_TOO_WIDE",
      "deontic": "MUST",
      "condition": "uncertainty_bounded == true",
      "effect_if_failed": "ABSTAIN",
      "reason_code": "uncertainty_too_wide",
      "artifact_required": true
    }
  ]
}
